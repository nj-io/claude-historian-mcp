# Dependencies
node_modules/
# ...including when it is a symlink, as git worktrees often need
node_modules

# Build output
dist/
*.tsbuildinfo

# Demo artifacts (keep only demo.gif)
demo/*.cast
demo/*.mp4
demo/*.mov
demo/.claude/

# Local configuration
.claude/*
!.claude/skills/
.mcp.json

# Git worktrees created for feature work
.claude-worktrees/

# Development
*.log
*.tmp

# OS
.DS_Store

# npm
*.tgz
*.dxt
isolate-*.log

# Generated docs
CLAUDE.md

# Eval fixtures derived from real conversation history.
# These contain real session ids and query strings lifted from private work;
# this is a public fork, so they must never be committed.
# queries.sample.json is synthetic and IS committed.
test/fixtures/queries.local.json
test/fixtures/baseline.local.json
